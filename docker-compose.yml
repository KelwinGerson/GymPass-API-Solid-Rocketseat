version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=docker-pg
      - POSTGRESQL_PASSWORD=dockerpg
      - POSTGRESQL_DATABASE=apisolid -p 